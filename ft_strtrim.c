#include "libft.h"
char *ft_strtrim(char const *s1, char const *set)
{
    char* s3;
    char* s2;
    int s1len;
    int setlen;
    setlen = ft_strlen(set);
    s1len = ft_strlen(s1);
    int i;
    while (
        s1[i] != set[i]
    )
    {
        
    }
    
}